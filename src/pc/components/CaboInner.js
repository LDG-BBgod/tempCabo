const CaboInner = ({ children }) => {
  return (
    <div
      style={{
        maxWidth: 1120,
        margin: 'auto',
        height: '100%',
      }}
    >
      {children}
    </div>
  )
}

export default CaboInner
